# Copy to .env.local. Credentials stay on the server and are ignored by Git.
TEAM_API_KEY=
LEASH_BASE_URL=https://leash-api-production.up.railway.app
VISECA_API_MODE=remote
PORT=3210

# Required for new instruction decoding in both purchase modes; saved activity remains readable without it.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4
AI_ENABLED=false
